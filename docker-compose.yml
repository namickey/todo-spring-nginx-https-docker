version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - SPRING_PROFILES_ACTIVE=default
    network_mode: host
  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    depends_on:
      - app
    network_mode: host
